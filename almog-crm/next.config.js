/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow the existing static HTML files to coexist when deployed
  // alongside this Next.js app if needed
}

module.exports = nextConfig
